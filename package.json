{
  "name": "failure-logic-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node static-server.js",
    "test:e2e": "npx playwright test --config=tests/playwright.config.js tests/e2e --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "express": "^4.22.1",
    "playwright": "^1.40.0",
    "serve": "^14.2.6"
  }
}
