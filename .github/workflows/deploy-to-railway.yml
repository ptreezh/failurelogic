name: Deploy to Railway

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Railway CLI
        run: npm i -g @railway/cli
        
      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        # Railway CLI v4+ reads RAILWAY_TOKEN env var directly; the
        # `railway login --token ...` form was removed in v4 and the
        # `railway up` step was failing with "unexpected argument '--token'".
        # Just run `railway up` — env var carries auth.
        run: railway up --service failure-logic-api --detach
          
      - name: Get deployment URL
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          railway status
          echo "DEPLOYMENT_URL=$(railway status | grep -o 'https://[^ ]*.up.railway.app' | head -1)" >> $GITHUB_ENV
          
      - name: Update deployment status
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'success',
              description: 'Deployed to Railway',
              context: 'deployment/railway'
            });
