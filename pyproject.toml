[project]
name = "failure-logic-api"
version = "1.0.0"
description = "FastAPI backend for Failure Logic cognitive-bias training game"
requires-python = ">=3.11"

# Dependencies live in api-server/requirements.txt — Vercel's @vercel/python
# builder auto-discovers them from the build src directory.
