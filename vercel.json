{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'using Vercel Python build'",
  "installCommand": "pip install --upgrade pip && pip install -r api-server/requirements.txt"
}
